{
  "name": "astrov7",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "wrangler types && astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "cf:build": "wrangler types && astro check && astro build",
    "cf:deploy": "wrangler types && astro check && astro build && wrangler deploy --config dist/server/wrangler.json",
    "cf:preview": "wrangler types && astro preview",
    "cf:types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.6",
    "@astrojs/preact": "^6.0.4",
    "@aws-sdk/client-s3": "^3.1121.0",
    "@aws-sdk/s3-request-presigner": "^3.1121.0",
    "@neon/config": "^1.1.0",
    "@neon/env": "^1.1.6",
    "@neondatabase/neon-js": "^0.7.0-beta",
    "@neondatabase/serverless": "^1.1.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.10",
    "files-sdk": "^2.3.0",
    "preact": "^10.29.8",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/ts-plugin": "^1.10.11",
    "@types/node": "^26.4.0",
    "typescript": "6",
    "wrangler": "^4.127.1"
  }
}